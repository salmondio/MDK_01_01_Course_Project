<UserControl x:Class="Course_project_wpf.Elements.SortableHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="800">

    <UserControl.Resources>
        <SolidColorBrush x:Key="BaseColor" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="Hint" Color="#666666"/>
        <SolidColorBrush x:Key="ActiveColor" Color="#333333"/>
    </UserControl.Resources>

    <Border Background="{StaticResource BaseColor}" 
            BorderBrush="#DADCE0" 
            BorderThickness="0,0,0,1"
            Padding="5,0,5,0"
            Height="40">
        <Grid x:Name="HeaderGrid"/>
    </Border>
</UserControl>